<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Custom_list_view_2"
        tools:targetApi="31">

        <!-- Only Launcher Activity -->
        <activity
            android:name=".login_page_c"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities (without launcher intent) -->
        <activity android:name=".custom_list_view_c" android:exported="true" />
        <activity android:name=".food_purchase" android:exported="true" />
        <activity android:name=".success_page" android:exported="true" />

    </application>

</manifest>
